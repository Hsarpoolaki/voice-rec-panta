Gradle Wrapper binaries are not bundled in this lightweight source export. Use Android Studio to sync/build.

SpeechRecognizer behavior depends on the device voice service. For multilingual testing, switch the recognizer locale in system settings if Arabic or English utterances are not being recognized well.
